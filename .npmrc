# NPM Configuration

# ensure consistent installs across environments
save-exact=false

# use npm's flat dependency tree (npm 7+)
legacy-peer-deps=false

# npm registry
registry=https://registry.npmjs.org

